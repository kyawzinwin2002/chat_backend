Send Code Service

1. Code Creation;
2. Send Email,

class CreateCode
{
    public static function create($receiver)
    {
        $code = 111111;
        Code::create([
            "code" => code;
            "receiver" => receiver;
            "code_type" => type;
        ])

        return $code;
    }
}

class SendCodeService
{
    public static function send($receiver,$email)
    {
        Mail::to($receiver->email)->send($email);
    }
}

class RegisterController
{
    public function register()
    {
        //Other Codes;

        //Send Code to User;
        $code = CreateCode::create($receiver);

        $email = new VerificationEmail($receiver,$code);

        SendCodeService::send($receiver,$email);
    }
}






